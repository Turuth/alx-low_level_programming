#include "main.h"
/**
 * add - adds two integers and returns the result
 * @x: The first number
 * @y: The second number
 * Return: addition of both numbers
 */
int add(int x, int y)
{
	return (x + y);
}
