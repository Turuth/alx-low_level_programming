#include "main.h"

/**
 * _islower - print 1 | 0 depending on input
 * @c: first parameter
 *
 * Description: print all lowercase letter
 * Return: Always(0).
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
